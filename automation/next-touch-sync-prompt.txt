You are running a scheduled sync for "Next Touch," Adam Russo's (adam.russo@bench.co) sales relationship tracker. This is a recurring hourly job — you have no memory of previous runs, so start by reading the system-state doc.

STEP 1 — Read state. Use the Projects tool (project_read) to read `claude/notes/next-touch-system-state.md` in the attached "Dashboard" project. It has: the live Next Touch dashboard URL, the Daily Priorities page URL, the contact-brief template/example URL, the "Team access to call knowledge" section (read this carefully — it's a required part of every brief page, not optional polish), which contacts already have brief pages, the sync watermark (timestamp of the last processed activity, plus a known watermark-gap caveat — read it), the Apollo starred-sequence priority tiers with campaign IDs, and the behavior rules for this system. Follow every rule in the doc exactly — they reflect Adam's explicit decisions. The most important ones: (a) ONE consolidated brief page per contact, permanently — a new call transcript is never a new page or a standalone note, it gets merged into the contact's existing page (see the "CALL HISTORY CONVENTION" comment on the contact-brief template, and the matching rule in the state doc); (b) never silently overwrite existing facts — flag every edit in the page's "Update log" section with a timestamp and one-line reason, and note inline what old content it replaced; (c) if a completed meeting still has no Gemini notes by end of day (after 6 PM Mountain Time), flag it visibly on the Next Touch dashboard; (d) replies get drafted in Gmail for Adam's review, never auto-sent; (e) priority is never a flat "X days = overdue" rule; (f) every brief page — new or existing — must carry the full verbatim call transcript (not just a link), the on-page search bar, and the share hint, per the "Team access to call knowledge" section; (g) every contact name anywhere on the Next Touch dashboard — new or existing card, This Week entries, any other list — must open that contact's Contact Brief modal popup, per the state doc's "Contact Brief modal" section (built 2026-08-26, replacing the old toggleRes/.resources panel entirely — that pattern is retired, never build a new toggleRes-style panel again): every name is wrapped in an element with `onclick="openBrief('<slug>')"`, the shared `#briefOverlay`/`#briefModal` markup and `openBrief`/`closeBrief` JS live once at the bottom of the page (don't duplicate them), and each contact's content comes from one JSON object in the `<script type="application/json" id="contacts-data">` blob (name/business/email/priority/tier/signal/lastContact/upcoming/notes/action/briefUrl/coach — where `coach` holds that contact's Call Coach content, see below). When adding a NEW contact, add an entry to that JSON blob (with a real `coach` object built the same way as existing ones — see next point) and give the card's name element the matching `onclick="openBrief('<new-slug>')"` — do not build bespoke modal markup per contact. Do NOT attempt to build any kind of in-page AI question-answering widget — the state doc confirms no capability supports that; the documented answer for open-ended questions is Adam or a teammate asking Claude directly in a session.
(h) Call Coach is now standard functionality for every contact, not a Nathan-Moser-only pilot (trial completed and rolled out to all contacts 2026-08-26). Every contact's modal must include a Call Coach section in its `coach` data (tailored talk-track — situation summary plus a handful of card-style talking points grounded in that contact's real notes/objections/transcript and the Call Verbiage Field Guide, never a generic script), and every standalone contact-brief page (built per Step 2/3 below) must include the same `<!-- ============ CALL COACH ============ -->` section with a `.callcoach-cta` button after the status pills, following the pattern already live on the Nathan Moser, Andrew Bravo, and Dana Hansen pages. Never gate this behind a contact allowlist or pilot flag again — build it for every contact from here forward, same as the transcript/search-bar/share-hint requirements in (f).

STEP 2 — Check for new inbound emails from tracked contacts since the watermark. Search Gmail for messages received after the watermark timestamp from contacts already on the Next Touch dashboard (read the dashboard artifact via Artifact action "read" to get the current contact list and their emails). Known gap (see state doc): a message can arrive earlier the same calendar day as a watermark was set and get missed by a strict "after watermark" search. To guard against this, ALSO pull today's (Adam's local Mountain Time date) messages from each tracked contact regardless of the watermark, and cross-check each one against what that contact's dashboard card / brief page currently says — if the dashboard doesn't yet reflect it, treat it as new even though its timestamp is before the watermark. For each new inbound message that is a real human reply (not an automated notification):
- Read the thread.
- Determine if it signals: wants to cancel / no longer interested, found another solution, is asking follow-up questions, has given new information that changes the next step, or anything else that changes the recommended next action. Call out explicitly which of these applies, or that it's a plain response otherwise.
- Update that contact's EXISTING brief page if they have one (Artifact action "read" with `url`, edit, republish with the same `url` — never create a new artifact for a contact who already has a page). If they don't have a brief page yet, build one now using the Nathan Moser page's full structure as the template (TL;DR callouts, chip lists, checklists, `<details>` for dense tables, back-to-Next-Touch link, share hint, on-page search bar + its script, "Previous calls" section, "Update log" section, same fonts/colors) — including the full verbatim transcript for this contact's call(s), pulled from their Gemini doc via Drive, not summarized — and add its URL to the state doc's contact table. Any time you change or replace existing content on the page (not just append), log it in the "Update log" section — timestamp, one-line reason, what it replaced. Never quietly overwrite a fact the client corrected.
- Move this contact to the top of today's priority tier on the Next Touch dashboard and state the reason in plain language (e.g. "Replied to follow-up email, asking about pricing") — never re-rank silently.
- Draft a suggested reply as a Gmail draft (create_draft) grounded in the contact's notes — addressed, ready to send, but never sent automatically.

STEP 3 — Check for completed meetings since the watermark. Search Calendar for meetings that have ended since the watermark. For each one, check Gmail/Drive for the Gemini "Notes by Gemini" transcript (email + linked Drive doc). If found, generate the post-call notes (Prospect Details, Summary Details, Action Items, Solution Setting). Then consolidate, don't fragment: open the contact's EXISTING brief page (or build one from the Nathan Moser template if this is their first, including the full team-access pattern from Step 1(f)). If they already have a "Last call" section from a prior call, move its current contents down into a new condensed, dated entry under "Previous calls" — including that call's own full-transcript `<details>` block, moved down with it, not deleted (TL;DR + top pain points + action items + the transcript is enough there), then replace "Last call" with this new call's notes, complete with its own newly-pulled full verbatim transcript in a `<details>` block. Merge any new or changed facts into the single "Full business profile" table rather than duplicating it — flag via the Update Log rule if a fact changed rather than was simply added. Regenerate the "Before your next call" pre-call-summary section from THIS newest call's open items and objections, not the older ones. Update the header status pills (priority, sequence step, next meeting) to match. If a meeting ended but no Gemini notes have landed yet, leave it for the next run rather than guessing at content — but check the current time (Bash `date`, Mountain Time): if it's after 6 PM MT and this meeting's notes still haven't landed by this run, add a clearly visible "⚠ Missing notes" flag for that contact directly on the Next Touch dashboard (not just logged in the state doc).

STEP 4 — Rebuild the Next Touch dashboard. Re-read the current dashboard HTML (Artifact action "read"), update it with anything found in Steps 2-3 (new priority ranking with stated reasons, updated "last contact" fields, updated notes excerpts, links to any newly-built contact briefs, any "⚠ Missing notes" flags), and republish it to the same URL. Keep its existing design system — don't redesign it. Every card you add or touch must follow the Step 1(g)/(h) conventions above (clickable name opening the shared Contact Brief modal, with Call Coach content included in that contact's `contacts-data` entry) — applies to every card, not just ones with changed content this run.

STEP 4a — Refresh the "This Week" section (added 2026-08-26). The dashboard now has a `<section class="thisweek" id="section-thisweek">` block, positioned between the "New this pass" notice and the High priority group, that lists Adam's prospect meetings for the current week grouped by day (today highlighted), each one matched against tracked Salesforce data and/or an existing Next Touch card. Refresh it every run, not just when something changed elsewhere:
- Fetch the live Pipeline Pulse artifact fresh (Artifact action "read" on https://claude.ai/code/artifact/ce5924e8-918b-4ea5-b039-3b5ae79a3d95) and parse its `#pp-state` JSON for `thisWeek`, `seenLeads`, `seenOpportunities`, `seenContacts`, `seenAccounts`. Pipeline Pulse's own hourly task keeps `thisWeek` current from Google Calendar — don't re-query Calendar yourself for this section, just consume what Pipeline Pulse already computed (its `thisWeek.checkedAtLabel` tells you how fresh it is; if it looks stale — more than a few hours old — you may pull Calendar directly as a fallback and note that you did).
- For each meeting in `thisWeek.days`, determine a match in this priority order: (1) does the attendee's email match a contact already on the Next Touch dashboard? If so, render it as "On Next Touch · <priority> priority" with a one-line summary of that contact's current situation and an in-page anchor link to their card (`#c-<slug>`) — don't duplicate their full card content here. (2) Else, does the email or normalized business name match an entry in `seenLeads`/`seenOpportunities`/`seenContacts`/`seenAccounts`? If so, show the matched Lead/Opportunity/Contact/Account fields (stage, account name, and its Salesforce link if one has been captured — most records still show "pending capture" per the Pipeline Pulse handoff, that's expected, don't fabricate a link). (3) Otherwise, render it as unmatched: a clearly visible flag (reuse the `.tw-match-none` style already on the page) stating plainly that this meeting is not confidently matched to a Salesforce record or an existing Next Touch contact, and compose a short pre-call review built only from real calendar details (meeting type, any client note field, reschedule history) plus general Verbiage Field Guide framing appropriate to that meeting type (first consultation vs. demo) — never invent business specifics you don't actually have. This is a direct, explicit rule from Adam: a meeting that can't be confidently matched must be flagged as such, never silently assigned to the wrong profile or given fabricated Salesforce context.
- Keep the section's existing CSS classes (`.tw-day`, `.tw-today`, `.tw-meeting`, `.tw-row1`, `.tw-time`, `.tw-name`, `.tw-match`, `.tw-match-nt`, `.tw-match-none`, `.tw-declined`, `.tw-meta`, `.tw-review`, `.tw-footnote`) — don't invent new ones for this section.

STEP 4b — Rebuild the Daily Priorities page. Re-read it (Artifact action "read" on the Daily Priorities URL in the state doc). Refresh it from the same live sources: today's/tomorrow's prospect-facing meetings (with prep notes) from Calendar, anything from Step 2 that needs a "Needs you now" callout, and the Apollo starred-sequence tier table (re-pull `overdue_manual_tasks_count` per tier via apollo_tasks_search). Update the date header. Keep the "no silent caps" convention for the high-volume lower tiers (state the aggregate count, don't itemize). Republish to the same URL. Keep its existing design system.

STEP 4c — Pipeline Pulse manual-refresh morning reminder (added 2026-08-26, per Adam's explicit request — "Ill ensure Next Touch reminds me of a morning task to refresh the chrome extension until I can migrate this into one desktop"). This step does NOT touch Salesforce itself and does NOT violate the "no Salesforce" rule below — it only reads a doc and writes a reminder banner. Only run this check when the current hour in Adam's local time is exactly 07 (Bash: `TZ='America/Edmonton' date +%H` equals "07" — this is the once-per-weekday-morning run, roughly 7:52 AM Mountain Time; skip this entire step on every other hourly run, don't add or remove anything outside this window). When it is that hour:
- Read the Pipeline Pulse project doc fresh (`claude/pipeline-pulse-handoff.md`, Projects tool `project_read`) and check whether its "Scheduled automation" section's "Current limitation (as of 2026-08-26): device not bound..." subsection is still present and still describes the device as unbound.
- If it IS still unbound: add or refresh a banner at the very top of the Daily Priorities page (above everything else, styled consistently with the page's existing design system) reading something like: "🔲 Manual Salesforce check needed today — Pipeline Pulse's automated Chrome refresh is still waiting on device binding. Before today's/tomorrow's meetings, search Salesforce by each attendee's email (see the Pipeline Pulse handoff doc's per-person lookup steps) and update the tracker by hand." Include a plain-text link to the Pipeline Pulse artifact (https://claude.ai/code/artifact/ce5924e8-918b-4ea5-b039-3b5ae79a3d95) so Adam can jump straight to it.
- If that Pipeline Pulse doc no longer describes the device as unbound (meaning Adam has since migrated to one desktop and approved binding): remove this banner from the Daily Priorities page instead — don't leave a stale reminder up once the real automation is working. Note in the state doc's Run log that the reminder was retired because binding succeeded.
- Either way, this only touches the Daily Priorities page's own banner — never write to Pipeline Pulse's state/artifact from this step, and never attempt any Salesforce action yourself; the banner is just a pointer telling Adam to do the lookup himself in a Chrome-connected chat.

STEP 5 — Update state. Edit `claude/notes/next-touch-system-state.md` in the Project: set the sync watermark to this run's start time, add any newly-built contact-brief URLs to the table, and append a brief entry to the Run log section at the bottom noting anything unusual (a contact whose Gmail/Calendar data conflicts with what's on file, an auth failure on any connector, a meeting still missing notes at end of day, a pre-watermark message caught by the Step 2 same-day check, whether the Step 4c morning reminder was shown/refreshed/retired today, whether the Step 4a This Week refresh found Pipeline Pulse data stale and fell back to a direct Calendar pull, etc.) — append, don't overwrite prior entries. If Gmail, Calendar, Apollo, or Google Drive fail to connect or authenticate, do not fail silently — log it clearly.

Do not touch Salesforce — there is no live connection; leave those fields as they are. (The Step 4c reminder banner is the one exception noted above — it only reads a doc and writes a banner, it never touches Salesforce or Pipeline Pulse's own state. Step 4a reads Pipeline Pulse's already-published state, it never writes to it.)

When publishing any Artifact republish (dashboard, Daily Priorities, or a contact brief), the Artifact tool may refuse an unseen-newer-version publish — if so, follow its error instructions exactly (read the saved file it points to in full, merge your edits onto it, then republish) rather than forcing or resending unchanged content.

If there is nothing new (no inbound emails, no completed meetings) since the watermark, still do Step 4a (This Week moves on its own as the week progresses and Pipeline Pulse's data changes) and Step 4b (Daily Priorities changes daily regardless of new activity — the meeting list and Apollo tier counts move on their own) and update the watermark and log "no new activity" — but don't republish the Next Touch dashboard unnecessarily beyond the This Week refresh, and don't touch any contact briefs unnecessarily. Exception: still run the end-of-day missing-notes check from Step 3 if it's after 6 PM MT, and still run the Step 4c morning-reminder check if it's the 07:00 MT hour, even on an otherwise quiet run.
